- unless @tasks.empty?
  div class="panel panel-default"
    div class="panel-heading" Tasks:
    table class="table"
      tr
        th Workers:
        th Created at:
        th Task:
        th State:
        th
      - @tasks.each do |task|
        tr id="task_#{task.id}"
          = render 'task', task: task
- else
  h4 No tasks.
